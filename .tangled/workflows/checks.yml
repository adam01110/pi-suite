when:
  - event: ["push", "manual"]
    branch: ["main"]
  - event: ["pull_request"]
    branch: ["main"]
engine: "nixery"
dependencies:
  nixpkgs/nixpkgs-unstable:
    - nix
steps:
  - name: "Run checks"
    command: |
      set -euo pipefail
      nix flake check
